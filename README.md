# python-batch-jan

##git commands

To clone a repository

   git clonr https url

To list branch

    git branch

To create a branch

    git checkout -b class1

To see latest local the changes
     
    git status

To excactly changes made on a single file
   
    git diff

to stage the changes:
   
   git add <filename>

to commit changes:
  
  git commit -m"commit changes"

To push changes
  
  git push origin<sourcebranch>
     ex:git push origin class1
  
### Daily 
  
  to check branch is clean
    
    git status

to checkout main branch

    git checkout mian

to get the latest changes
  
   git pull origin main

to create a new branch
    
    git checkout -b<new branch name>